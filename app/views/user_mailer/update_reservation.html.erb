<div>
  <p>Hi, <%= "#{@booking.user.first_name} #{@booking.user.last_name}" %></p>
  <p>Your Flight Booking to <%= "#{@booking.flight.to_airport.name} from #{@booking.flight.from_airport.name} on #{@booking.flight.date.strftime('%B %d, %Y')} has been successfully updated. Below are the new flight details" %></p>

  <h3>Your Information </h3>
  <hr/>
  
  <% if @booking.passengers.any? %>
    <h4>Passengers Information</h4>
    <hr/>
    <ol>
      <% @booking.passengers.each do |passenger| %>
        <li><%= "#{passenger.first_name} #{passenger.last_name}" %></li>
      <% end %>
    </ol><br/>
  <% end %>

  <p><b>Total Fare: </b> <%= @new_cost %> </p>
</div>
